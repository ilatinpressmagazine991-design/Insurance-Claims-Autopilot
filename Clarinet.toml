[project]
name = 'Insurance-Claims-Autopilot'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.adjudication]
path = 'contracts/adjudication.clar'
clarity_version = 3
epoch = 'latest'

[contracts.intake-and-triage]
path = 'contracts/intake-and-triage.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
